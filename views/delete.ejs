<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Cancel</title>
  </head>
  <body class="bg-gray-100 py-8">
    <div class="max-w-md mx-auto bg-white rounded-lg overflow-hidden md:max-w-lg">
      <h1 class="text-2xl font-bold text-gray-800 p-4 bg-gray-100">Delete Appointment</h1>
      <p class="p-4">Are you sure you want to delete this appointment?</p>
      <div class="p-4">
        <p><span class="font-bold">Title:</span> <%= appointment.title %></p>
        <p><span class="font-bold">Description:</span> <%= appointment.description %></p>
        <p><span class="font-bold">Start Time:</span> <%= appointment.start %></p>
        <p><span class="font-bold">End Time:</span> <%= appointment.end %></p>
      </div>
      <form method="POST" action="/appointments/<%= appointment.id %>?_method=DELETE" class="p-4">
        <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
          Delete
        </button>
      </form>
    </div>
  </body>
</html>
