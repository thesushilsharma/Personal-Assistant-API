<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manage Appointment</title>
  </head>
  <body>
    <h1>Update Appointment</h1>
    <form method="POST" action="/appointments/<%= appointment.id %>?_method=PUT">
      <div>
        <label for="title">Title:</label>
        <input type="text" name="title" value="<%= appointment ? appointment.title : '' %>" />
      </div>
      <div>
        <label for="description">Description:</label>
        <textarea name="description"><%= appointment ? appointment.description : '' %></textarea>
      </div>
      <div>
        <label for="start">Start Time:</label>
        <input type="datetime-local" name="start" value="<%= appointment.start.toISOString() %>"  />
      </div>
      <div>
        <label for="end">End Time:</label>
        <input type="datetime-local" name="end" value="<%= appointment.end.toISOString() %>" />
      </div>
      <input type="hidden" name="_method" value="PUT" />
      <button type="submit">Update</button>
    </form>
    <a href="/appointments">Back to Appointments</a>
  </body>
</html>
